name: Reqit Security Checks

on:
  pull_request:
  workflow_dispatch:

jobs:
  reqit-security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate generated tests and assertions
        run: node tests/scan.runner.js --security
